<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude
        domain="sharedpref"
        path="applozic_preference_key.xml" />
    <exclude
        domain="sharedpref"
        path="PARTSEAZY_PREFERNCES.xml" />


    <exclude domain="database" path="MOEInteractions"/>

    <exclude domain="sharedpref" path="pref_moe.xml"/>
</full-backup-content>
